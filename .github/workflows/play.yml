# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: "Battle"

on:
  issues:
    types: [opened]

jobs:
  action:
    runs-on: ubuntu-latest
    if: startsWith(github.event.issue.title, 'battle|')
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dart
        uses: dart-lang/setup-dart@v1
      - name: run-game
        run: dart run lib/main.dart "${{ github.event.issue.title }}" "${{ github.event.issue.user.login }}"
      - name: Push change commit
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Apply run-game changes
      - name: Close Issue
        uses: peter-evans/close-issue@v2
        with:
          comment: Your move Success - Auto Close Issue
      - name: The job has failed
        if: ${{ failure() }}
        uses: peter-evans/close-issue@v2
        with:
          comment: Your move Failure - Auto Close Issue